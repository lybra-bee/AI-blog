name: Build, Generate Content and Deploy Hugo Site

on:
  schedule:
    - cron: "0 6 * * *"   # каждый день в 6 утра UTC
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Клонируем репозиторий
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Устанавливаем Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      # 3. Устанавливаем зависимости
      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install requests Pillow

      # 4. Запускаем генератор (с API ключом)
      - name: Run content generator
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: python content_generator.py

      # 5. Коммитим новые статьи и изображения (если есть)
      - name: Commit new content
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add content/posts/ static/images/gallery/
          git commit -m "📝 Автоматическая генерация статьи и изображения [CI]" || echo "Нет изменений"
          git push

      # 6. Устанавливаем Hugo Extended
      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.148.0'
          extended: true

      # 7. Устанавливаем Node.js (для PostCSS, если нужно)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          if [ -f package.json ]; then
            npm ci
          fi

      # 8. Генерация Hugo сайта
      - name: Build Hugo site
        run: hugo --minify --destination ./public --logLevel=debug

      # 9. Публикуем на GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
