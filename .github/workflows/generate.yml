name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches:
      - main   # 🚀 Деплой запускается при пуше в main

permissions:
  contents: write   # нужно для публикации в gh-pages

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          submodules: true   # подтянет тему Hugo
          fetch-depth: 0     # нужно для правильного рендера

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.133.0'  # можешь поменять на актуальную
          extended: true

      - name: Build
        run: hugo --minify

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages

